# 全局快捷键备用入口验收记录

## 验收状态

- 日期：2026-07-16
- 状态：自动化、系统接口、核心中英文界面及快捷键切换验证通过；多显示器和全屏空间待确认
- 对应需求：[全局快捷键备用入口需求](requirements-global-shortcut.md)
- 对应设计：[全局快捷键备用入口技术设计](design-global-shortcut.md)

## 验收环境

- macOS 26.5（25F71）
- Apple Silicon `arm64`
- Xcode macOS SDK 26.5
- Swift 6 / Swift Testing
- 临时应用包：`/private/tmp/SSH Tunnel Manager-dev.app`

## 正式安装

- 已将 `0.3.0` 覆盖安装到 `/Applications/SSH Tunnel Manager.app`。
- `CFBundleShortVersionString` 和 `CFBundleVersion` 均为 `0.3.0`。
- Bundle ID 为 `io.github.mobius1024.ssh-tunnel-manager`。
- `codesign --verify --deep --strict` 验证通过。
- 已从正式安装路径启动，并确认运行进程来自 `/Applications/SSH Tunnel Manager.app`。

## 英文专项验证

- 英文代表性界面文案、运行时错误、快捷键冲突文案、快捷键格式化和中英文资源键一致性共 5 项测试通过。
- 正式安装版已使用进程级 `AppleLanguages=(en)` 和 `AppleLocale=en_US` 参数启动；该设置只对本次进程生效，不修改 macOS 全局语言。
- 英文界面的实际布局、底部固定操作和快捷键切换行为已完成实机确认。
- 英文测试结束后已无语言覆盖参数重新启动正式应用，恢复跟随 macOS 系统语言。

当前命令执行环境无法读取图形显示器画面，`screencapture` 返回“could not create image from display”，因此涉及实际窗口外观、全屏空间和多显示器定位的项目保留为人工确认，未把无画面证据写成通过。

## 已完成验证

### 自动化测试

执行：

```bash
swift test
```

结果：88 项测试全部通过。新增覆盖包括：

- 默认 `⌃⌥⌘T` 设置和稳定 JSON 编码。
- 非法组合、仅 Shift、修饰键和不支持键码校验。
- `settings.json` 原子读写、未知版本处理和 `0700`/`0600` 权限。
- 启动默认注册、系统冲突查询失败、独占注册冲突、一般注册失败和无修改重试。
- 候选注册成功后再保存和切换。
- 持久化失败时注销候选并保留旧快捷键。
- 停用保存失败时保留活动快捷键。
- 录制当前活动快捷键时回填组合且不展示主界面。
- 录制期间按键状态变化和组合修饰键映射。
- 活动快捷键连续触发时逐次调用展示切换动作。
- 异常设置文件使用默认值并在保存后修复。
- 英文和简体中文本地化 key 集合一致。

### Release 构建与签名

执行应用包构建脚本生成临时 release 应用。验证结果：

- release 编译通过。
- `codesign --verify --deep --strict` 通过。
- `Info.plist` 保持 `LSUIElement=true`。
- 应用包没有增加辅助功能或输入监控用途声明。
- 可执行文件正确链接 Carbon、AppKit 和 SwiftUI。

### 系统快捷键查询

使用独立只读探针调用 `CopySymbolicHotKeys`：

- 返回状态 `0`。
- 返回的键码和修饰键使用 Carbon 编码，与生产实现的比较方式一致。
- 生产实现只比较已启用条目，并过滤与产品无关的修饰键位。

### 默认快捷键真实注册

临时 release 应用运行时，独立进程尝试以独占方式注册 `⌃⌥⌘T`：

```text
-9878
```

该值为 `eventHotKeyExistsErr`，证明应用已经真实独占注册默认组合。停止临时应用后，同一探针返回 `0`，证明退出后注册已经释放。

### 重新打开与单实例

对已运行的临时应用再次执行打开操作后：

- 应用进程 PID 保持不变。
- 进程数量仍为一个。
- 没有创建第二个应用实例或第二套 `TunnelManager` 进程状态。

窗口是否实际展示并置前仍需在真实桌面画面中确认。

### 回归与静态检查

- `git diff --check` 通过。
- 新增和修改文件未发现行尾空白。
- 现有隧道模式、SSH 参数、配置兼容、端口检测和退出清理测试全部通过。
- `.vscode/` 是开发前已有的未跟踪目录，本次没有修改。
- 面板不再通过超大临时高度测量内容；标题和底部操作固定，只有中间隧道列表滚动，避免顶部裁切或底部操作消失。

### 实机确认

- 面板顶部内容完整显示，隧道较多时可在面板内部滚动。
- 录制无冲突快捷键后可以保存，保存成功时设置窗口自动关闭。
- 快捷键第一次触发展示主界面，第二次触发关闭，第三次重新展示。

### 纯图标回归（待实机确认）

- 菜单栏项目使用菜单栏厚度作为固定宽度并显示纯图标，不再常驻占用文字宽度。
- 悬浮提示和辅助功能标签显示动态名称及运行数量。
- 面板顶部标题和底部“添加隧道/退出”固定，中间仅隧道列表滚动。

### 冲突提示回归

- 保存失败与本次候选快捷键绑定，不依赖当前仍生效的旧快捷键运行状态。
- 已确认冲突时显示醒目的红色提示，明确说明新组合未保存且原快捷键仍然有效。
- 系统快捷键被 macOS 优先消费时，录制器通过录制期间的短时按键状态查询保留候选组合，返回设置界面后仍可执行冲突检测。

## 等待人工确认

请在当前真实桌面环境中完成以下最小检查：

1. 正常退出并重新启动，确认自定义组合恢复。
2. 在全屏空间和外接显示器中触发，确认面板位于当前交互屏幕。
3. 确认全过程没有辅助功能或输入监控授权提示。

以上项目确认后，需求和技术设计文档可以更新为“已验收”。
